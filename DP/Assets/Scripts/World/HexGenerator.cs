namespace ConvoyManager.World
{
    /// <summary>
    /// Вспомогательный класс для процедурной генерации сетки гексов.
    /// В текущей реализации логика перенесена в WorldState.Generate.
    /// </summary>
    public static class HexGenerator
    {
        // При необходимости сюда можно вынести логику расчёта координат и типов тайлов.
    }
}